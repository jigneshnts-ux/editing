export function createSubtitle(): HTMLParagraphElement {
  const text = document.createElement('p');
  text.textContent = 'Create news posts, reels, thumbnails, and scene-based content from one workspace.';
  return text;
}
